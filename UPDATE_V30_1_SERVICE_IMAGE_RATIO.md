# V30.1 — Service image ratio fix

- Chỉ sửa khung ảnh popup chi tiết Dịch vụ.
- Bỏ `min-height: 420px` gây khung cao hơn ảnh.
- Chuẩn hoá stage về tỉ lệ `16:9` trên desktop, tablet và mobile.
- Giữ `object-fit: contain` + căn giữa để ảnh không bị crop hoặc kéo méo.
- Tăng cache version CSS lên `30.1`.
